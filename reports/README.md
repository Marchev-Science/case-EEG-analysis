# reports/

Drop your report here as `<your-name>.md` (or `<your-name>.pdf`).

Use [`TEMPLATE.md`](TEMPLATE.md) as the starting point.

Figures go in `reports/figures/<your-name>/` so multiple students don't collide.
